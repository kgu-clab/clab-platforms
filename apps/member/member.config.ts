import { Config } from '@type/config';

/**
 * members 플랫폼에 대한 설정을 정의합니다.
 * 자세한 설정 방법은 공식 문서를 참고 바랍니다.
 */
const config: Config = {
  name: 'C-Lab',
  loginUrl: 'https://auth.clab.page',
};

export default config;
